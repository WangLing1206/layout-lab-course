## 重做《关键亮点说明》：1 页 / 6 个核心亮点 / 3 张截图

### 一、为什么要重做（诊断）

当前这份文档（10 个小节、17KB、导出 7 页）有两个问题：

1. **定位偏移**——它其实是「工作日志」：需求对照表、18 个演示清单表、目录树、逐条验收记录、许可。这些对开发有用，但对评审是噪音。作业第 4 项要的是「说明关键亮点」。
2. **没有一张图**——这是一个视觉与交互类项目，却全靠文字描述。你自己的两版早期作品（`E:\page_layout\.shots\`、`可视化导论\artifacts\`）都带截图，说明你本来也认为该有。加上 pandoc 默认 LaTeX 的密集表格与两端对齐，就是「排版丑」的直接来源。

### 二、交付物（替换现有三个文件）

```
submission/
├─ 关键亮点说明.docx      ← 设计版，单页 A4（新的唯一设计源）
├─ 关键亮点说明.pdf       ← 由上面这份 docx 经 LibreOffice 导出（与 docx 完全一致）
├─ 关键亮点说明.md        ← 纯文本源，更新为同一内容
└─ img/                   ← 3 张截图（提交入库，md 在 GitHub 上也能正常显示）
```

> 现有 `.docx/.pdf` 被 `.gitignore` 排除，不入库；截图与 `.md` 入库。

### 三、6 个核心亮点（每条 2 句话讲透，全部对得上作业要求）

| # | 亮点 | 对应要求 |
|---|---|---|
| 01 | **案例驱动**：「远山手记」博客首页改造前「能用但不好读」，五章各解决一个阶段，前一步的结论是后一步的依据；每章侧栏带案例进度指示 | 加分项 |
| 02 | **每个知识点固定「讲解 / 关键代码 / 动态演示」三栏**：等宽切换器而非从上到下堆叠；无 JS 时自动堆叠、内容不丢 | 硬要求 |
| 03 | **18 个真实可交互演示**：滑块 / 分段按钮 / 开关 / 可点击画板，全是真实 DOM；画板会实时校验区域矩形合法性并说明原因 | 硬要求 |
| 04 | **演示下方是「跟着你重写的 CSS」**：每个演示都有一块实时输出代码块随操作同步重写，调满意直接复制 | 差异化亮点 |
| 05 | **中英双语全站一致**：正文 / 控件 / 代码注释 / 演示读数 / 实时代码注释全部随语言切换，911 词条、自检 0 漏译 | 硬要求 + 难点 |
| 06 | **网站本身就是所讲方法的示范**：原生 HTML/CSS/JS 无框架无构建；Grid 搭骨架、Flex 做零件、`clamp()` 流体字号、容器查询用在讲解面板自身 | 示范性 |

**删掉**：需求对照表、18 个演示清单表、目录树、逐条验收记录、许可。这些要么是噪音，要么属于 README。验收只保留页脚一行数字。

### 四、3 张截图

| 图 | 内容 | 证明什么 |
|---|---|---|
| 图 1 | 首页（案例主线 + 五章入口 + 「18 个知识点 / 18 个演示」） | 亮点 01、02 |
| 图 2 | 第 1 章「留白」的**动态演示**栏：滑块 + 实时读数 + 下方实时输出代码块 | 亮点 03、04 |
| 图 3 | **双语对照**：同一节的中文版与 EN 版并排拼成一张（用 Pillow 合成 2-up） | 亮点 05 |

底部做成 3 张一行的图带，配居中灰字图注。图 1 与图 2 用固定视口（1440×900）抓取并裁到目标区域，保证文字在缩到约 55mm 宽时仍可辨认。

### 五、版式设计（单页 A4）

- **页边距** 上下 14mm / 左右 16mm；字体 Microsoft YaHei + Segoe UI，等宽用 Consolas
- **页头**：主标题「布局实验室 · Layout Lab」（accent 色 #1F6B64，与站点同色）+ 一行定位语 + 在线地址；下压一条 accent 细线
- **主体**：6 个亮点排成 **2 列 × 3 行卡片表格**，每格 = accent 色编号 + 加粗小标题 + 两行说明；用极浅底色（#F4F7F6）做卡片底纹，不画重边框
- **图带**：3 张图一行 + 居中灰字图注
- **页脚**：一行小字——原生 HTML/CSS/JS · 无构建 · 5 章 18 知识点 18 演示 · 911 词条 0 漏译 · 仓库地址
- 目标是**留白充足、层级清楚**，而不是把纸填满

### 六、实现步骤

1. 用浏览器在线上站点按固定视口抓 3 张截图，裁切到目标区域；双语对照用 Pillow 合成 2-up
2. 用 `python-docx` 生成设计版 DOCX：自定义字号/颜色、accent 细线、卡片表格、单元格底纹（`w:shd` XML）、图片插入
3. 用 `E:\libreoffice\program\soffice.exe --headless --convert-to pdf` 导出 PDF（带 `-env:UserInstallation` 避免 profile 冲突）
4. **验收门**：用 `pdftoppm` 把 PDF 渲染成 PNG 并目检——确认**恰好 1 页**、中文无缺字、配色与图片正常、图内文字可读；溢出则收紧字号/裁短文案直到 1 页
5. 更新 `.md` 源，提交截图与 `.md`，docx/pdf 按 `.gitignore` 不入库

### 七、需要注意的点

- **1 页偏紧**：6 张卡片 + 3 张图 + 页头页脚塞进 A4 会挤。我会先按紧凑参数排，若溢出优先压缩说明文字与图高，而不是缩到看不清。
- **DOCX 保真度**：python-docx 画的版式，LibreOffice 导出 PDF 时极少数底纹/边框可能有细微差异。若出现明显走样，备选方案是改用「设计版 HTML → Chrome 无头打印 PDF」，代价是 DOCX 无法完全对齐（届时会告诉你）。
- 我会先确认 `E:\libreoffice\program\soffice.exe` 能正常无头转换（当前 plan 模式下不能执行它）。
- **不改动**：网站本身、README、视频脚本、压缩包（`layout-lab-course-源码.zip` 不含 submission 之外的变化，无需重建）。